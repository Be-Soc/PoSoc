# Флаппинг вокруг freeze/hold и расщепление мгновенных видов

- **Класс:** liveness / стабильность выводимого состояния.
- **Серьёзность:** высокая.
- **Уверенность:** интерпретация (механизм); расхождение мгновенных видов узлов — факт §2.5; осцилляции частично признаны §5.2.2 и §7.1.
- **Предусловия:** атакующий контролирует пороговое число голосов C1 или связей C2 в окрестности ребра; способен точно тайминговать свои записи.
- **Механизм:**
  1. Условия ребра удерживаются чуть меньше $T_{hold}$, затем срываются (отзыв `MEMBER_OF` или провал пары) и восстанавливаются — окно «этого $S$» перезапускается (§5.5, bootstrap п.3); freeze не приостанавливает накопление окна (§5.5 «Freeze ∥ hold window»).
  2. Отрыв происходит мгновенно, возврат — через $T_{hold}$ (асимметрия §7.1): флаппинг подавляется затратами атакующего на повторное включение, но не запрещён.
  3. Из-за разницы доставок узлы в один момент вычисляют разные состояния («в каждый отдельный момент узлы имеют субъективные виды состояния», §2.5) — статус ребра между узлами расходится на интервалах порядка задержек.
  4. Дрейф порогов при росте брата (§10.11) даёт дополнительные точки срыва «бесплатно» — без подписей атакующего.
- **Воздействие:** отложенная/невозможная активация рёбер у честных групп (livelock пересчётов), расходящиеся виды у потребителей доверия на границах порогов; «осцилляция REVOKE↔`MEMBER_OF` самонаказуема» (§5.2.2) — цена атакующего ненулевая.
- **Задетые разделы спеки:** §2.5, §5.2.2, §5.5–5.6, §7.1, §10.11.
- **Возможные митигации:** монотонный идентификатор среза состояния (snapshot id) для сходимости видов, минимальный стабильный интервал между пересчётами статусов, лимит числа перезапусков окна на группу за период, гистерезис порогов (зона нечувствительности вокруг t).
