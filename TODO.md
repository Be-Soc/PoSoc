# TODO: известные несогласованности, унаследованные из ветки wrong (v0.13)

Перенос выполнен дословно по источникам ветки wrong (specs/concept.md,
specs/implementation.md, specs/glossary.md); выявленные внутренние
несогласованности источников фиксировались здесь до решения владельца.

**Открытых проблем нет.** Оба пункта решены в spec v0.13:

1. Нотация `FRIEND_REVOKE` согласована с конвенцией подписанта (§2.3, §2.8,
   §3.2, §13): payload обеих парных записей — каноническая пара `{x, p}`
   с x < p, подписант задаётся только `sigs` (`[r]`, r ∈ {x, p} — отзывающий);
   направление разрыва и адресат poison mark определяются подписантом
   (правило стрелки: `FRIEND_REVOKE(A→B)` — подписант A, mark против B).
2. Оценка подписной нагрузки (§8) пересчитана для бинарных объединений
   (D = log₂(N_pop/|L0̄|) ≈ 25 при N_pop = 10⁹, |L0̄| = 30 — §5.6.3, §9):
   ≈ 120–130 тихих записей/год.

## v0.14: разделение модели ключей

Модель ключей разделена на два класса: identity-ключ человека (единственный
участник протокола, выводится из seed: мнемоника BIP-39 + passphrase + KDF,
§2.1, §8.4) и device-ключ (транспортный, вне протокола, §8.5). Сертификаты
устройств и реестры устройств отнесены к транспортному уровню экосистемы —
спецификация «transport-identity» суперрепозитория BeSoc (в этой спеке
упомянута текстово, без относительной ссылки) — и здесь не нормативируются.
Открытых проблем нет: набор протокольных записей и форматы (§2.3, §2.8)
не изменены.

Известные уточнения на следующие ревью (не блокируют v0.14):

1. Граница градаций §7.3.1(а)/(б) после введения контракта инициализации
   (§8.4) опирается на то, что secure storage реально противостоит извлечению
   identity-секрета; полное извлечение секрета — уже случай (б). Порог между
   (а) и (б) стоит проговорить явнее.
2. Ротация «новым путём деривации из того же seed» (§7.3.4) предполагает
   повторную инициализационную церемонию (ввод seed) на устройстве; сама
   церемония деривации при ротации спекой не описана — противоречия нет,
   процедуру можно специфицировать позже.
