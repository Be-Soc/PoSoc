# TODO: известные несогласованности, унаследованные из ветки wrong (v0.13)

Перенос выполнен дословно по источникам ветки wrong (specs/concept.md,
specs/implementation.md, specs/glossary.md); выявленные внутренние
несогласованности источников фиксировались здесь до решения владельца.

**Открытых проблем нет.** Оба пункта решены в spec v0.13:

1. Нотация `FRIEND_REVOKE` согласована с конвенцией подписанта (§2.3, §2.8,
   §3.2, §13): payload обеих парных записей — каноническая пара `{x, p}`
   с x < p, подписант задаётся только `sigs` (`[r]`, r ∈ {x, p} — отзывающий);
   направление разрыва и адресат poison mark определяются подписантом
   (правило стрелки: `FRIEND_REVOKE(A→B)` — подписант A, mark против B).
2. Оценка подписной нагрузки (§8) пересчитана для бинарных объединений
   (D = log₂(N_pop/|L0̄|) ≈ 25 при N_pop = 10⁹, |L0̄| = 30 — §5.6.3, §9):
   ≈ 120–130 тихих записей/год.
